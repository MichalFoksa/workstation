<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>net.michalfoksa.demo.workshop</groupId>
		<artifactId>workstation</artifactId>
		<version>0.0.16</version>
		<relativePath>..</relativePath>
	</parent>

	<artifactId>workstation-api</artifactId>
	<description>API library of workstation-service</description>
	<packaging>jar</packaging>

	<dependencies>

		<!-- BTW: Having spring-boot-starter-web dependency would cover all module 
			needs. -->
		<!-- <dependency> 
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId> 
        </dependency> -->

		<!-- Minimal dependencies for code generated by Swagger spring-boot library -->
		<dependency>
			<groupId>javax.validation</groupId>
			<artifactId>validation-api</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-web</artifactId>
		</dependency>

		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>

		<dependency>
			<groupId>io.swagger</groupId>
			<artifactId>swagger-annotations</artifactId>
			<version>${swagger.annotations.version}</version>
		</dependency>

		<!-- servlet-api and slf4j-api are needed by default API interface implementations -->
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>servlet-api</artifactId>
			<version>2.5</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<!-- Spring Boot plugin isn’t necessary in this module because the 
				main function of it is to create an executable über-jar, which we don’t want 
				and don’t need for a library. -->
			<!-- <plugin> <groupId>org.springframework.boot</groupId> <artifactId>spring-boot-maven-plugin</artifactId> 
				</plugin> -->

			<!-- Swagger codegen -->
			<plugin>
				<groupId>io.swagger</groupId>
				<artifactId>swagger-codegen-maven-plugin</artifactId>
				<version>${swagger-codegen-plugin.version}</version>
				<executions>
					<execution>
						<goals>
							<goal>generate</goal>
						</goals>
						<configuration>
							<inputSpec>${project.basedir}/src/main/resources/swagger/workstation-api.yaml</inputSpec>
							<language>spring</language>
							<apiPackage>${default.swagger.package}.api</apiPackage>
							<modelPackage>${default.swagger.package}.model</modelPackage>
							<invokerPackage>${default.swagger.package}.handler</invokerPackage>
							<configOptions>
								<library>spring-boot</library>
								<dateLibrary>java8</dateLibrary>
								<serializableModel>false</serializableModel>
								<interfaceOnly>true</interfaceOnly>
								<hideGenerationTimestamp>true</hideGenerationTimestamp>
								<useTags>true</useTags>
								<sourceFolder>src/main/java-generated</sourceFolder>
							</configOptions>
							<output>${project.basedir}</output>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- Delete src/main/java-generated as part of generate-sources and clean 
				goals -->
			<plugin>
				<artifactId>maven-clean-plugin</artifactId>
				<configuration>
					<filesets>
						<fileset>
							<directory>${project.basedir}/src/main/java-generated</directory>
						</fileset>
					</filesets>
				</configuration>
				<executions>
					<execution>
						<id>delete-src/main/java-generated</id>
						<!-- 'initialize' is the phase before 'generate-sources' use 'verify' 
							to execute it before 'install' -->
						<phase>initialize</phase>
						<goals>
							<goal>clean</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

		</plugins>
	</build>

</project>